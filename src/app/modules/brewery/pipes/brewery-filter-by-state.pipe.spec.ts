import { BreweryFilterByStatePipe } from './brewery-filter-by-state.pipe';

describe('BreweryFilterByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new BreweryFilterByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
