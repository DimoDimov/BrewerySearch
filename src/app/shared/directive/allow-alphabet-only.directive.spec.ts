import { AllowAlphabetOnlyDirective } from './allow-alphabet-only.directive';

describe('AllowAlphabetOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowAlphabetOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
